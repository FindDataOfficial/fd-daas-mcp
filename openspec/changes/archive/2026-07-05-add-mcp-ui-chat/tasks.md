## 1. composite-mcp demo UI tool (Python)

- [x] 1.1 Confirm `mcp/composite-mcp/pyproject.toml` lists `ui_tools` under `[tool.setuptools] py-modules` (no new Python dep — pure FastMCP); `uv sync` in `mcp/composite-mcp/` succeeds
- [x] 1.2 Create `mcp/composite-mcp/ui_tools.py` with `render_stock_summary(symbol: str)` — builds an HTML widget (stat tiles + a one-line summary), registers a FastMCP `@app.resource("ui://composite-mcp/stock-summary/{symbol}", mime_type="text/html;profile=mcp-app")` resource template returning the HTML string, and the tool returns `ToolResult(content=[TextContent(type="text", text=...)], meta={"ui": {"resourceUri": f"ui://composite-mcp/stock-summary/{symbol}"}})` via FastMCP's `ToolResult.meta` (→ `CallToolResult._meta`)
- [x] 1.3 Register `render_stock_summary` and the resource template in `mcp/composite-mcp/server.py` (always-present, like the management tools); confirm `FilterTools` does not strip it from proxied composites
- [x] 1.4 Manually verify: `cd mcp/composite-mcp && uv run fastmcp run server.py --no-banner` then call `tools/list` (has `render_stock_summary`), `tools/call render_stock_summary {symbol:'AAPL'}` (returns `_meta.ui.resourceUri`), and `resources/read ui://composite-mcp/stock-summary/AAPL` (returns `text/html;profile=mcp-app`)
- [x] 1.5 Add a self-check `mcp/composite-mcp/selfcheck_ui_tool.py` (temp/stub server) asserting the tool returns `_meta.ui.resourceUri` and the resource is readable; run `uv run --directory mcp/composite-mcp python selfcheck_ui_tool.py`

## 2. Dashboard dependencies + raw MCP client

- [x] 2.1 Add to `dashboard/package.json`: `@mcp-ui/client`, `@modelcontextprotocol/ext-apps`, `@modelcontextprotocol/sdk` (pin exact versions); run `npm install` in `dashboard/`; verify `import { AppRenderer } from '@mcp-ui/client'` resolves
- [x] 2.2 Add `dashboard/src/lib/mcp-ui-server.ts` — server-side singleton raw `Client` (`@modelcontextprotocol/sdk`) per server, using `StdioClientTransport` with spawn config reused from `mcp-client.ts`'s `getServerConfig`; export `getMCPClientRaw(server)` and `getMCPClientRawTools(server)` (returns AI-SDK-shaped tools mapping `client.listTools()` → `{description, inputSchema, execute: (args) => client.callTool({name, arguments: args})}`)
- [x] 2.3 Add `dashboard/src/app/api/mcp-ui/[op]/route.ts` — `read-resource` (body `{server, uri}` → `client.readResource({uri})`), `call-tool` (body `{server, name, arguments}` → `client.callTool(...)`), `list-resources` (body `{server}` → `client.listResources()`); same-origin only; 405 on non-POST
- [x] 2.4 Add a self-check script (or jest/vitest test) in `dashboard/` that stubs a raw `Client` and asserts the `/api/mcp-ui/read-resource` route returns the `UIResource` JSON for `ui://composite-mcp/stock-summary/AAPL`

## 3. /api/chat server dispatch

- [x] 3.1 Update `dashboard/src/lib/mcp-client.ts` — keep `getMCPClient`/`getMCPTools` (`@ai-sdk/mcp`) for non-composite servers; add `getMCPClientRaw`/`getMCPClientRawTools` re-export from `mcp-ui-server.ts`; export `getServerConfig` for the raw client to reuse. Leave `defaultServer()` as `leader-mcp` (global default for non-chat callers like the workflows + collections APIs); the chat-specific composite-mcp default lives in `chat/page.tsx` (`DEFAULT_SERVER`) and `api/chat/route.ts` (`mcpServer`) so it doesn't ripple to those callers
- [x] 3.2 Update `dashboard/src/app/api/chat/route.ts` — read `server` from the incoming request body/metadata; if `server === 'composite-mcp'` (or default), use `getMCPClientRawTools(server)` for `streamText`; else use the existing `getMCPTools(server)` `@ai-sdk/mcp` path; keep the `rawStreamResponse` fallback consistent
- [x] 3.3 Verify the leader-mcp path still works: send a chat request with `server: 'leader-mcp'` and confirm ECharts code blocks still render (no regression) — `useRawClient = mcpServer === 'composite-mcp'` so `leader-mcp` uses `getMCPTools('leader-mcp')` unchanged; ECharts rendering is client-side (`EchartsBlock`); `agents.cy.ts` + `workflows.cy.ts` pass (no regression)

## 4. Chat UI: server selector + UI-resource rendering

- [x] 4.1 Create `dashboard/src/components/chat/ui-resource-block.tsx` — a client component that takes `{server, toolName, toolResourceUri, toolInput, toolResult}` and renders `<AppRenderer>` with NO `client` prop, supplying `onReadResource`/`onCallTool`/`onListResources` handlers that `fetch('/api/mcp-ui/<op>', {method:'POST', body: JSON.stringify({server, ...})})`; `sandbox` = `{ sandboxOptions: 'allow-scripts' }` (no `allow-same-origin`); `onOpenLink` opens `window.open(url, '_blank', 'noopener')`; shows a non-blocking error placeholder on fetch failure
- [x] 4.2 Update `dashboard/src/components/chat/message-list.tsx` (and/or `tool-call-card.tsx`) — for each tool-result part, detect `output._meta?.ui?.resourceUri` OR a `UIResource` content type in `output.content`; if present, render `<UiResourceBlock>` instead of `<ToolCallCard>`; otherwise render `ToolCallCard` unchanged
- [x] 4.3 Add the server selector to `dashboard/src/app/chat/page.tsx` — a `<select>` populated from `.mcp.json` `mcpServers` keys (or a static list: `composite-mcp`, `leader-mcp`, `daas-mcp`, `cron-mcp`, `dashboard-mcp`, `alerts-mcp`); default `composite-mcp` (or `MCP_SERVER` env override); persist selection in `localStorage`
- [x] 4.4 Wire the selector into the chat transport — use `TextStreamChatTransport`'s `prepareSendMessages` (or a custom body) to include `{server}` on every request to `/api/chat`; verify `/api/chat` reads it
- [x] 4.5 Manual E2E in the browser: open `/chat`, confirm selector shows `composite-mcp`; ask "show me a stock summary for AAPL"; confirm `render_stock_summary` fires, the tool result renders as a sandboxed iframe widget (not a plain card), and the widget HTML is visible — **verified up to the API layer** (`/api/mcp-ui/read-resource` returns the `text/html;profile=mcp-app` UIResource via a real composite-mcp stdio spawn; `/api/mcp-ui/call-tool` returns `_meta.ui.resourceUri`; `next build` compiles `/chat` + `/api/mcp-ui/[op]` cleanly). The final browser→LLM→tool-call→AppRenderer-iframe step requires an LLM API key (none in this env) and is left for the user to confirm with `npm run dev`.

## 5. Verification & docs

- [x] 5.1 Run `openspec validate --changes add-mcp-ui-chat` (must pass) and `openspec status --change add-mcp-ui-chat` (all `applyRequires` done)
- [x] 5.2 Run dashboard E2E: `cd dashboard && npx cypress run` (existing specs must pass; add a `chat.cy.ts` case for the selector + UI-resource rendering if the suite covers `/chat`) — full suite via `node scripts/run-e2e.mjs`: all 8 specs pass (`agents`, `collections`, `dashboard`, `mcp-ui` (new), `process`, `scores`, `settings`, `workflows`); no regression. The suite doesn't cover `/chat` (no LLM in CI), so no `chat.cy.ts` added; `mcp-ui.cy.ts` covers the `/api/mcp-ui/*` route contract.
- [x] 5.3 Update `construction/dashboard.md` and `CLAUDE.md` (composite-mcp section + dashboard section) with the mcp-ui rendering flow, the new `/api/mcp-ui/*` routes, the `render_stock_summary` demo tool, and the server-selector default switch to `composite-mcp`
- [x] 5.4 Note in the change a follow-up: wire live data (akshare) into `render_stock_summary`, and add a self-check asserting `_meta.ui.resourceUri` survives a proxied upstream round-trip once an upstream UI tool exists
