## 1. scrapling-uv-mcp script-runner tools

- [x] 1.1 In `mcp/scrapling-uv-mcp/server.py`, add a script-directory resolver: read `SCRAPLING_SCRIPTS_DIR` from env (root `.env` already loaded), default to `mcp/scrapling-uv-mcp/scripts/scrapers/`; resolve to an absolute path; `mkdir(parents=True, exist_ok=True)` lazily inside the tools.
- [x] 1.2 Implement `find_scripts` tool (register with the existing `@server.tool()` pattern): list every `*.py` in the resolved dir; for each return `name` (stem), `path` (abs), and `summary` (first line of the module docstring, else first `#` comment line, else empty string); never execute scripts.
- [x] 1.3 Implement `run_script` tool (`@server.tool()`): resolve target as `<dir>/<name>.py`; reject `name` containing `/`, `\`, or `..` (path-traversal guard); run `subprocess.run([sys.executable, script_path, *args], cwd=<scrapling-uv-mcp dir>, capture_output=True, timeout=<SCRAPLING_SCRIPT_TIMEOUT or 120>)`; return `{returncode, stdout, stderr}` as text. Unknown script → error, no execution.
- [x] 1.4 Add `SCRAPLING_SCRIPTS_DIR` (and `SCRAPLING_SCRIPT_TIMEOUT`) to root `.env` with a comment, defaulting to the in-repo scrapers dir.
- [x] 1.5 Add a runnable self-check (`python server.py --selfcheck` or a `selfcheck.py`) that points at a temp script dir, writes a tiny `echo.py`, and asserts: `find_scripts` lists it with a summary, `run_script` returns `returncode=0` with the expected stdout, traversal names are rejected. Must not touch `mcp/daas.db`.
- [x] 1.6 Restart scrapling-uv-mcp and confirm both tools are advertised (e.g., via the `fastmcp` skill / `mcp` list-tools); run the self-check green.

## 2. `extract-web-data` project-scope skill

- [x] 2.1 Use the `fd-skill-creator` skill to scaffold `.claude/skills/extract-web-data/` with `SKILL.md` (YAML frontmatter: name, description, trigger rules) and an optional `references/` dir.
- [x] 2.2 Write the SKILL.md workflow as the canonical steps: (a) inspect URL via scrapling-uv-mcp `fetch`, fall back to `stealthy_fetch` on challenge/blocked response; (b) generate a self-contained Scrapling scraper; (c) save to `SCRAPLING_SCRIPTS_DIR` as `<slug(config_name)>.py`, refuse overwrite unless user confirms; (d) persist config via `mcp/scrapling-uv-mcp/scripts/db_helper.py` `save_config(name, url, columns)`; (e) verify via `run_script`; (f) register the website as a managed daas datasource (see 2.6/2.7) — auto-deciding the category level; (g) on opt-in, schedule via cron-mcp: ensure a cron task exists (`list_db_tasks` → `create_task` or `update_task`, command `uv run --directory <abs>/mcp/scrapling-uv-mcp python <abs>/<slug>.py`, timeout), then `create_schedule(name="scrape_<slug>_<freq>", cron=<expr>, task="scrape_<slug>")` (reuse existing via `list_schedules`), then `run_now` once and read `list_executions`; report the schedule id.
- [x] 2.3 Include a compact scraper template (inline in SKILL.md or `references/scraper-template.md`) the skill emits: imports `scrapling`, extracts the agreed columns, prints JSON to stdout, accepts optional `--out <path>`.
- [x] 2.4 Document prerequisites in SKILL.md: `SCRAPLING_SCRIPTS_DIR`, and that scrapling-uv-mcp + daas-mcp + cron-mcp must be enabled in `.mcp.json` / `settings.local.json`.
- [x] 2.5 Note the relationship to `fd-daas-scraw-scrapling` (one-shot fetch→generate→save) and that this skill adds run + daas-registration + schedule, reusing the same `scraw_configs` table and script dir.
- [x] 2.6 Define the shared-slug convention and daas registration step: datasource `name` = `scraw_configs.name` = script stem = `<slug>`; `create_datasource(name=<slug>, label, url, config_json=`{"scraw_config": "<slug>", "script": "<abs path>"}`, category_id=<from 2.7>)`. Idempotent: `search_datasources(source_name=<slug>)` first → `update_datasource` if it exists.
- [x] 2.7 Define the auto-level rule (no user prompt): `get_category_tree()` → find-or-create root `Web Scraped` → find-or-create child named after the URL's registered domain (e.g. `example.com`) → pass that leaf's `category_id`. Find-or-create must be idempotent (match existing by name before `create_category`). Document the rule + the domain-extraction step in SKILL.md (or `references/auto-level.md`).
- [x] 2.8 Define the form/section attachment step: `add_form(source_name=<slug>, form_type="page", label=<title/url>)` → `add_section(form_id, section_name="columns", instruction=<column list>)`, so fields are queryable via `search_datasources(section="columns")`.

## 3. End-to-end verification

- [x] 3.1 Run the skill against a simple public URL end-to-end: confirm a saved `<slug>.py` in `SCRAPLING_SCRIPTS_DIR`, a `scraw_configs` row via `db_helper.get_config(name)`, a `run_script` call returning `returncode=0` with JSON stdout, and (opt-in) a cron schedule plus a successful immediate `run_now` (output read via `list_executions`).
- [x] 3.2 Confirm the daas registration: `search_datasources(source_name=<slug>)` returns the datasource; `get_category_tree()` shows it under `Web Scraped` → `<domain>` at depth 2; `list_forms(<slug>)` shows the `page` form with a `columns` section whose `instruction` carries the column list; re-running the skill for the same site updates in place (no duplicate datasource/category).
- [x] 3.3 Confirm rollback safety: removing the two tools and the skill dir leaves scrapling-uv-mcp fetch tools, `scraw_configs`, daas-mcp, and cron-mcp fully functional; any created schedules are deletable via cron-mcp `delete_schedule`, cron tasks via `delete_task`, and any created daas datasources via daas-mcp `delete_datasource` (orphaned categories via `delete_category`).
