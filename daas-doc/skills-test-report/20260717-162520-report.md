# fd-daas-* skills test+repair report

Generated: 20260717-162520
Harness: `.claude/skills/fd-daas-skill-review/scripts/skill_smoke_test.py`
DB snapshot before pass: `daas.db.bak-pre-skills-test`

## Summary

- **In-scope skills:** 14 (`fd-daas-*` with a `SKILL.md`)
- **L1 (static):** 14/14 pass
- **L2 (functional):** code-path verified for all; real offline L2 on the core
  fetch/indicator scripts (see below). Network-gated happy-paths relaxed to
  code-path checks per the `fd-daas-skills-test-suite` spec.
- **Routing-drift candidates:** none
- **Clean-exit criterion:** met (every in-scope skill passes L1+L2 with zero open defects)

## Defects found and fixed

| Skill | Class | Detail | Fix (via `fd-coding-skill-creator` edit path) |
| --- | --- | --- | --- |
| fd-daas-fetch-data | `script-bug` | referenced `dispatch.py` / `run_indicator.py` under the old `skill-based-data-fetch` path (2) | renamed `skill-based-data-fetch` -> `fd-daas-based-data-fetch` throughout |
| fd-daas-indicators-creator | `script-bug` | referenced `upsert.py` under the old `skill-based-data-fetch` path | renamed `skill-based-data-fetch` -> `fd-daas-based-data-fetch` throughout |
| fd-daas-dashboard | `script-bug` | `scripts/register_dashboard.py` missing (script lives in `fd-daas-dashboard-creator`) | pointed both refs at `.claude/skills/fd-daas-dashboard-creator/scripts/register_dashboard.py` |

## Harness false-positives resolved (no skill edit)

During the pass the L1 harness produced false positives that were fixed in the
harness, not the skills (the skills were correct documentation):

- `fd-daas-based-data-fetch` flagged 5x `stale-ref` for naming removed CLIs in
  its "No `fd-*` CLI binaries" do-not-use list -> harness now skips removed-surface
  mentions on negative-cue lines (`no`/`removed`/`do not`/`gone`/...).
- `fd-daas-skill-creator` flagged `stale-ref` for `fd-daas-workflow-creator` in a
  do-not-reference list that wrapped across lines -> harness now also checks the
  previous line for a negative cue.
- `fd-daas-indicators-creator` flagged `stale-ref` table `ADD` from prose
  "`ALTER TABLE ADD COLUMN`" -> harness dropped the `TABLE` DDL keyword from the
  table-reference regex (keeps `FROM`/`JOIN`/`INTO`/`UPDATE`).
- `mcp__*` detection tightened to the real per-source tool-name shape
  (`mcp__src__tool`), so bare `mcp__*` / `mcp__` prose mentions are not flagged.
- `scripts/x.py` reference matching requires a token start, so a sibling-skill
  path like `fd-daas-skill-review/scripts/...` in prose is not mistaken for the
  skill's own script.

## L2 functional

- **Real offline L2 (produced the promised artifact):**
  - `fd-daas-based-data-fetch`: `dispatch.py --resolve akshare_stock_zh_a_hist`
    -> returned the import/call-shape snippet (promised output). ✓
  - `fd-daas-based-data-fetch` / `fd-daas-indicators-creator`: `run_indicator.py
    --list-ops` -> returned the op catalog. ✓
  - `fd-daas-indicators-creator` / `fd-daas-fetch-data`: `run_indicator.py --calc
    scraw_spy_daily date Close sma window=5` -> `{indicator: sma, count: 259,
    dates, values}` (promised ad-hoc calc output). ✓
- **Code-path verified (scripts import + run on --help/--list-ops/--resolve):**
  every referenced script in every in-scope skill (the harness run-check).
- **Network-gated L2 relaxed to code-path check** (recorded): live
  akshare/yfinance/edgar/edinet/dartlab fetches, live dashboard HTML build, live
  PDF ingest, live Scrapling scrape. The underlying tool layer for these is
  covered by the `fd-daas-mcp` pytest suite (81 tests green - see
  `daas-doc/mcp-test-suite.md`).

## Per-skill status

| Skill | L1 | L2 | Defects | Notes |
| --- | --- | --- | --- | --- |
| fd-daas-based-data-fetch | pass | code-path + real | 0 (after harness fix) | core fetch scripts run offline |
| fd-daas-dashboard | pass | code-path | 0 (1 fixed) | register_dashboard.py path fixed |
| fd-daas-dashboard-creator | pass | code-path (network-gated) | 0 | live dashboard build relaxed |
| fd-daas-entities-collection | pass | code-path | 0 | sync covered by MCP suite |
| fd-daas-entities-collection-creator | pass | code-path | 0 | |
| fd-daas-fetch-data | pass | code-path + real | 0 (2 fixed) | stale skill path renamed |
| fd-daas-indicators-collection-creator | pass | code-path | 0 | |
| fd-daas-indicators-creator | pass | code-path + real | 0 (1 fixed) | stale skill path renamed; --calc verified |
| fd-daas-pdf | pass | code-path (network-gated) | 0 | live ingest relaxed; pdf MCP covered |
| fd-daas-research | pass | code-path | 0 | research_tools covered by MCP suite |
| fd-daas-rules-creator | pass | code-path | 0 | rule engine covered by MCP suite |
| fd-daas-scrapling-official | pass | code-path (network-gated) | 0 | live scrape relaxed |
| fd-daas-skill-creator | pass | code-path | 0 (after harness fix) | new meta-skill |
| fd-daas-skill-review | pass | code-path | 0 | new meta-skill; harness self-tested |

## Excluded

- `fd-coding-daas-datasource-builder-workspace` - non-skill eval workspace (no
  `SKILL.md`); flagged for a removal decision, not tested as a skill.
- All `fd-coding-*` dirs - out of scope (not `fd-daas-*` data skills).
