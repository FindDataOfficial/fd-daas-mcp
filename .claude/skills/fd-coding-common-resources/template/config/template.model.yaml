# ============================================
# Single Model Entry Template
# Paste only this block when defining one model.
# The id is used by agents/workflows to reference this model.
# ============================================

- id: my-model
  provider: anthropic
  name: claude-sonnet-4-6
  max_tokens: 4096
  temperature: 0.7
