# Database URL (PostgreSQL, MySQL, or SQLite)
# SQLite (default): the harness registry DB at yfinance-agent-harness/.../metadata/registry.db
# PostgreSQL: postgresql://user:pass@host:5432/yfinance
# MySQL: mysql+pymysql://user:pass@host:3306/yfinance
YFINANCE_DATABASE_URL=

# Override the default registry DB path (defaults to the harness metadata/registry.db)
