# hkreport-mcp is keyless out of the box (HKEXnews + akshare HK endpoints
# are public). Set proxy env if you need to route outbound traffic.
# HTTP_PROXY=http://127.0.0.1:7892
# HTTPS_PROXY=http://127.0.0.1:7892
