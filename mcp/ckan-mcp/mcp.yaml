# ckan-mcp — MCP Server Configuration
# Run: uv run fastmcp run server.py --no-banner

name: ckan-mcp
type: stdio
command: uv
args:
  - run
  - fastmcp
  - run
  - server.py
  - --no-banner
cwd: /Users/chengsishi/code/cli-anything/mcp/ckan-mcp
env:
  DAAS_DATABASE_URL: sqlite:///../daas.db
  CKAN_PORTAL_URL: https://data.gov/api/3/
