# DAAS Scraw DB — SQLite schema for scraped URL configs
# This is the .env file for fd-daas-scraw-scrapling skill

# Database URL (SQLite default, relative to project root)
DAAS_SCRAW_DATABASE_URL=sqlite:///mcp/dass.db

# Scrapling Docker image
SCRAPLING_DOCKER_IMAGE=scrapling-mcp
