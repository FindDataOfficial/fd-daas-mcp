[project]
name = "trading-mcp"
version = "0.1.0"
description = "Multi-Agent Trading Analysis MCP — famous investor personas + TradingAgents pipeline"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0",
    "crewai>=0.80",
    "pydantic>=2.0",
    "python-dotenv>=1.0",
]

[tool.setuptools]
py-modules = ["server", "schemas"]
