[project]
name = "edinet-mcp"
version = "0.1.0"
description = "edinet-tools (Japan EDINET) MCP Server — entities, filings, parsed reports"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.0",
    "edinet-tools>=0.7",
    "pandas>=1.0",
    "python-dotenv>=1.0",
]
