# SEC EDGAR identity — set "Name email@domain". Required for live calls.
# (Unset here so root .env wins via load_dotenv order.)

# HTTP/HTTPS proxy for outbound SEC requests (optional)
HTTP_PROXY=http://127.0.0.1:7892
HTTPS_PROXY=http://127.0.0.1:7892
